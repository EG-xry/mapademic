# Search v2 + Coauthor Rays + z10 Slim Implementation Plan (R5)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Eric's morning fixes: (1) searches like "terrence sejnowski" and "aljadeff" must find "Terrence J. Sejnowski"/"Johnatan Aljadeff" (both ARE in the data — first-name-only bucketing + naive matching lose them); (2) search and Connect become distinct: search highlights ONE author AND draws their coauthorship rays (via the degree-of-separation backend); Connect stays as is; (3) drop z10 edge baking (Eric-approved hosting compromise; ambient edges stay at z8-9, per-author rays replace baked lines at max zoom).

## Global Constraints

- Repo `/Users/eric/Downloads/mapademic`, branch data-pipeline. `.venv/bin/pytest`. TDD for pipeline changes. No em dashes; no Claude co-author. Live browser verification for viewer changes.
- Verified facts: "Terrence J. Sejnowski" (994 works) and "Johnatan Aljadeff" (40 works) exist in coords_web. Search dir currently 821MB. Backend /api/graph/expand?new_id=&depth=1&top_k=25&edges=coauthor streams SSE (top_k max 25); implementer must read backend/graph_expand.py + frontend consumption for exact event shapes before coding.

### Task 1 (pipeline): dual-token search shards

`build_search_shards` v2 in pipeline/build_index.py: each author is indexed under the prefix family of the FIRST token of the normalized name AND the LAST token (skip the second insertion when first==last or the name has one token). Entry format unchanged. The recursive split + catch-all logic applies to the union. Entries may thus appear in two different shard files; the viewer dedupes by id. Update the module docstring contract. Tests: "terrence j sejnowski" reachable via both "terre..." and "sejno..." shard families; single-token names not duplicated; lossless invariant becomes "union of unique ids = input ids" and "each id appears 1-2 times". Commit "feat(index): dual first/last-token search shards".

### Task 2 (viewer): token-aware matching + dedupe

web/dev.html rankSearchEntries: replace prefix/substring logic with token matching. tokenize(norm) = split on spaces. A query matches an entry if every query token, in order, prefix-matches a distinct entry token (subsequence over tokens): "terrence sejnowski" matches "terrence j sejnowski"; "j sejnowski" matches; "sejnowski" matches (single token, matches any position). Ranking: full-prefix-of-name matches first (entry norm starts with the full query norm), then first-query-token-matches-first-entry-token, then others; cited desc within groups; dedupe by entry id (dual-bucketing) before ranking; top 12. Shard lookup unchanged (the concat-prefix chain now works for surname queries because Task 1 buckets by last token too — but ONLY after the production index is rebuilt; note in the code comment). Live-verify AFTER Task 5's index rebuild: "terrence sejnowski", "sejnowski", "aljadeff", "johnatan aljadeff" all find the right people; "maria" still fast. Commit "feat(web): token-aware search matching with id dedupe".

### Task 3 (viewer): search draws coauthor rays; search/Connect decoupled

On search select (and a "Coauthors" button on the info card): fly-to + highlight as now, then if the backend health probe passes, call /api/graph/expand?new_id=<bareId>&depth=1&top_k=25&edges=coauthor via EventSource; collect coauthor author nodes (exact event shapes from reading backend/graph_expand.py; filter works/non-author nodes); resolve coords via id shards; draw a "coauthors" GeoJSON source: thin rays from the author to each coauthor (accent #7fae8e, width 1.2, opacity 0.55) + small dot markers with name labels on the endpoints (textContent, pointer-events none, collision-free cap 25). Status pill near the search box: "top N coauthors" or "coauthor backend offline" (reuse the health probe; do NOT block highlight on backend). Rays cleared on: new search, Escape (highlight rung), Connect Go (path replaces rays), info card close does NOT clear rays. EventSource gets onerror like the path one. Generation-guard the async chain (searchRayGen). Coexistence: rays and path never both shown (Go clears rays; drawing rays clears any path via clearPath). Help overlay copy updated: search = explore one researcher + their coauthorships; Connect = shortest path between two. Live-verify with backend up: select a known author (use the cached Hinton), rays render to real coauthors, pill counts, Escape clears, Go replaces with path. Commit "feat(web): coauthor rays on search select; search/connect decoupled".

### Task 4 (pipeline): cap baked edges at z9

pipeline/tiles.py: EDGE_MAXZ = 9 module constant; regular edges drawn when EDGE_MINZ <= z <= EDGE_MAXZ (was z >= EDGE_MINZ); w1 layer stays z == MAXZ gated but production simply won't pass --edges-w1 (leave capability + tests). Update help text and the two edge tests that render at MAXZ to render at EDGE_MAXZ (add one test that z == MAXZ draws NO regular edges even when passed). dev.html help overlay: update the "one-off collaborations at max zoom" sentence to say per-author coauthor rays replace baked lines at max zoom (fold into Task 3's commit if cleaner, else here). Commit "feat(tiles): cap baked edges at z9 (z10 stays edge-free)".

### Task 5 (controller): production rebuild + QA + wrap

Delete data/tiles/10, re-render `tiles --zooms 10` (no edge flags; cached pixels_z10.parquet) -> measure new tiles total (target: total hosting < 10GB incl the larger dual-token search dir). Rebuild index (search shards only change). Live-verify Eric's exact queries + rays + a path. Composites/screens, Lavish update + reply, whole-branch review, ledger/memory.
